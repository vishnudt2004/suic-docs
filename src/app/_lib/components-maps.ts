import { ComponentType } from "react";
import { IconType } from "react-icons";

export const iconsMap: Record<string, IconType> = {};

export const componentExamplesMap: Record<string, ComponentType> = {};
